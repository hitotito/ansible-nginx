---
galaxy_info:
  author: Hitoshi Okonogi
  description: role to install nginx and configure
  company: hitotito
  license: MIT
  min_ansible_version: 2.0
  platforms:
    - name: Debian
      versions:
        - jessie
        - stretch
  galaxy_tags:
    - nginx
    - reverseproxy

dependencies:
  - role: hitotito.custom-facts
    custom_facts_name: hitotito_nginx
    custom_facts_file: nginx.fact
